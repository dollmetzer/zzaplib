# settings for the unit tests

# list of switchable languages. First entry is default
languages[] = "de"
languages[] = "en"

# list of switchable themes. First entry is default
themes[] = "web"

# optional module settings
[modules]
that[] = 'index'
this[] = 'index'
